<!DOCTYPE html>
<html lang="en">

<%- include('./partials/head') %>

<body>
    
    <%- include('./partials/nav.ejs') %>    

  <p>Hello, <%= title %> ! Welcome to our website.</p>

    <header>
      <h1>Welcome to the Home Page EJS</h1>
    </header>
    
    <main>
      <p>This is the main content of the home page.</p>
    <% if (blogs && blogs.length > 0) { %>
        
      <% blogs.forEach(blog  =>{ %>
            <h1 class="title"><%= blog.title %></h1>
            <p class="snipet"><%= blog.content %></p>
        <% }); %>
        
    <% } else { %>

        <h1> there are no blogs over here </h1>
      <% } %>


    </main>

    <%- include('./partials/footer.ejs') %>
</body>
</html>
